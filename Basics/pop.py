data=[100,300,500,450,600]
print(data)
data.pop(0) #In () Parentheses write the index of the set
print(data)
data.pop() # If we not give index than the pop function delete the last element
print(data)
data.pop(-1) # If we give index -1 than the pop function delete the last element
print(data)
data.pop(-2) #If we give any index with minus so the pop function delete the index you enter from last
print(data)