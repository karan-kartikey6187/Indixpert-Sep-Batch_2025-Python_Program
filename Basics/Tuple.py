# A tuple in Python is an ordered collection of items that cannot be changed (immutable).
# It is written inside parentheses ( ) and can store different types of data.
# We cant change the value
data=(1,2,3,4,5,6,7,12,32,79,50)
print(sum(data))